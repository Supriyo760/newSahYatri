services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgres://user:password@db:5432/sahyatri
      - NEXT_PUBLIC_API_URL=http://localhost:3000
      - AUTH_TRUST_HOST=true
      - AUTH_SECRET=9210c4f8d9b2a6f1e8a3c5d7b6a9e1f8
      - NEXTAUTH_SECRET=9210c4f8d9b2a6f1e8a3c5d7b6a9e1f8
    depends_on:
      - db
      - ml-service

  ml-service:
    build:
      context: ./python-service
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=sahyatri
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
